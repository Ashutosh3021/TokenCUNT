---
phase: 02-cli-tool
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: true
requirements: [REQ-09, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-15, REQ-16]
user_setup: []
must_haves:
  truths:
    - "ts ask command works with prompt and --file"
    - "ts analyze command works with file input"
    - "ts batch command processes JSON task list"
    - "ts report command shows session details"
    - "ts session command manages sessions"
    - "All commands support --dry-run (default True)"
    - "All commands support --json output"
  artifacts:
    - path: "src/tokencunt/cli/commands/ask.py"
      provides: "ask command implementation"
    - path: "src/tokencunt/cli/commands/analyze.py"
      provides: "analyze command implementation"
    - path: "src/tokencunt/cli/commands/batch.py"
      provides: "batch command implementation"
    - path: "src/tokencunt/cli/commands/report.py"
      provides: "report command implementation"
    - path: "src/tokencunt/cli/commands/session.py"
      provides: "session command implementation"
  key_links:
    - from: "src/tokencunt/cli/commands/*.py"
      to: "src/tokencunt/cli/formatters.py"
      via: "OutputFormatter for display"
    - from: "src/tokencunt/cli/commands/*.py"
      to: "src/tokencunt/core"
      via: "API client, token counter, session manager"
    - from: "src/tokencunt/cli/app.py"
      to: "src/tokencunt/cli/commands"
      via: "app.command() registration"
---

<objective>
Implement all 5 CLI commands: ask, analyze, batch, report, session

Purpose: Full CLI functionality with all commands
Output: Working CLI with all commands functional
</objective>

<context>
@src/tokencunt/cli/app.py
@src/tokencunt/cli/formatters.py
@src/tokencunt/cli/exit_codes.py
@src/tokencunt/core/__init__.py
@.planning/phases/02-cli-tool/02-CONTEXT.md

<interfaces>
From src/tokencunt/core:
- MiniMaxApiClient: API client class
- TokenCounter: Token estimation and tracking
- Session, SessionManager: Session tracking
- BudgetManager, BudgetConfig, BudgetStatus: Budget management
- RequestRecord: Request logging
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ask command</name>
  <files>src/tokencunt/cli/commands/ask.py</files>
  <action>
Create src/tokencunt/cli/commands/ask.py:

Function: ask_command(prompt: str, file: Optional[Path], dry_run: bool = True, ...)
- If --file provided, read content as prompt
- Use TokenCounter to estimate tokens (default behavior)
- If dry_run=True (default): show estimate, return without API call
- If dry_run=False: call MiniMaxApiClient.chat() and display response
- Display token costs (estimated/actual) using formatters
- Show budget status after call

Key flags:
- prompt: str (positional argument)
- --file, -f: Optional file input
- --dry-run: bool (default True per CONTEXT)
- --model: str (default from config)
- --json: Use json output mode
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.commands.ask import ask_command; print('ask command loads')"</automated>
  </verify>
  <done>ask command registered and functional</done>
</task>

<task type="auto">
  <name>Task 2: Implement analyze command</name>
  <files>src/tokencunt/cli/commands/analyze.py</files>
  <action>
Create src/tokencunt/cli/commands/analyze.py:

Function: analyze_command(file: Path, dry_run: bool = True, ...)
- Read file content
- Construct analysis prompt (system: "Analyze this code for improvements...")
- Use TokenCounter to estimate tokens
- dry_run shows estimate, actual sends to API
- Display token costs and budget status
- Response should include suggestions formatted as Rich Table

Key flags:
- file: Path (required positional)
- --dry-run: bool (default True)
- --focus: str (optional - what to analyze: bugs, performance, style)
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.commands.analyze import analyze_command; print('analyze command loads')"</automated>
  </verify>
  <done>analyze command registered and functional</done>
</task>

<task type="auto">
  <name>Task 3: Implement batch command</name>
  <files>src/tokencunt/cli/commands/batch.py</files>
  <action>
Create src/tokencunt/cli/commands/batch.py:

Function: batch_command(file: Path, dry_run: bool = True, ...)
- Read JSON file with array of prompts/tasks
- Use RequestBatcher to combine requests efficiently
- Show total estimated tokens for batch
- dry_run shows summary, actual processes all
- Display progress (Rich progress bar)
- Save results to output file or display

Input format (JSON):
```json
{"tasks": [{"prompt": "..."}, {"prompt": "..."}]}
```

Key flags:
- --input, -i: JSON file with tasks
- --output, -o: Optional output file
- --dry-run: bool (default True)
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.commands.batch import batch_command; print('batch command loads')"</automated>
  </verify>
  <done>batch command registered and functional</done>
</task>

<task type="auto">
  <name>Task 4: Implement report command</name>
  <files>src/tokencunt/cli/commands/report.py</files>
  <action>
Create src/tokencunt/cli/commands/report.py:

Function: report_command(session_id: Optional[str], ...)
- Load session from SessionManager
- Display usage summary using Rich Table:
  - Total requests
  - Total input tokens
  - Total output tokens
  - Total cost (if pricing known)
- Show budget status (used/limit/percentage)
- List recent requests as table

Key flags:
- --session: str (optional, default latest)
- --format: str (table, json)
- --json: JSON output mode
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.commands.report import report_command; print('report command loads')"</automated>
  </verify>
  <done>report command registered and functional</done>
</task>

<task type="auto">
  <name>Task 5: Implement session command</name>
  <files>src/tokencunt/cli/commands/session.py</files>
  <action>
Create src/tokencunt/cli/commands/session.py:

Function: session_command(action: str, ...)
Actions:
- new: Create new session
- list: List all sessions
- show: Show current session details
- config: Set budget/model for session
- clear: Clear session data

Key flags:
- action: str (new|list|show|config|clear)
- --budget: int (set budget limit)
- --model: str (set model)
- --json: JSON output mode
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.commands.session import session_command; print('session command loads')"</automated>
  </verify>
  <done>session command registered and functional</done>
</task>

<task type="auto">
  <name>Task 6: Wire all commands to app</name>
  <files>src/tokencunt/cli/app.py</files>
  <action>
Update src/tokencunt/cli/app.py to register all commands:

```python
from tokencunt.cli.commands import ask, analyze, batch, report, session

app.command()(ask.ask_command)
app.command()(analyze.analyze_command)
app.command()(batch.batch_command)
app.command()(report.report_command)
app.command()(session.session_command)
```

Add entry point to pyproject.toml:
[project.scripts]
ts = "tokencunt.cli.app:app"
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli import app; print('All commands wired')"</automated>
  </verify>
  <done>All 5 commands registered to Typer app</done>
</task>

</tasks>

<verification>
- [ ] ask command works with prompt and --file
- [ ] analyze command works with file input
- [ ] batch command processes JSON task list
- [ ] report command shows session details
- [ ] session command manages sessions
- [ ] --dry-run flag works (default True)
- [ ] --json flag works for all commands
- [ ] Global flags (-q, -v, --debug) work
</verification>

<success_criteria>
All 5 CLI commands functional with Rich formatting, dry-run default, and JSON output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-tool/02-02-SUMMARY.md`
</output>

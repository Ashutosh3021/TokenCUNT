---
phase: 02-cli-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-09, REQ-10, REQ-15, REQ-16]
user_setup: []
must_haves:
  truths:
    - "CLI entry point 'ts' works"
    - "Global flags (-q, -v, --json, --debug) functional"
    - "Rich formatters available for commands"
    - "Exit codes defined and exported"
  artifacts:
    - path: "src/tokencunt/cli/__init__.py"
      provides: "CLI package exports"
    - path: "src/tokencunt/cli/app.py"
      provides: "Typer app with global options"
    - path: "src/tokencunt/cli/exit_codes.py"
      provides: "Exit code constants"
    - path: "src/tokencunt/cli/formatters.py"
      provides: "OutputFormatter class"
  key_links:
    - from: "src/tokencunt/cli/app.py"
      to: "src/tokencunt/cli/formatters.py"
      via: "import OutputFormatter"
    - from: "src/tokencunt/cli/app.py"
      to: "src/tokencunt/core"
      via: "import core modules"
---

<objective>
Set up Typer CLI framework with basic structure, global flags, and Rich formatters.

Purpose: Foundation for all CLI commands
Output: CLI package structure with working entry point
</objective>

<context>
@src/tokencunt/core/__init__.py
@src/tokencunt/core/session.py
@src/tokencunt/core/budget.py
@src/tokencunt/core/token_counter.py
@.planning/phases/02-cli-tool/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Typer and Rich dependencies</name>
  <files>pyproject.toml</files>
  <action>
Add to [project.dependencies]:
- "typer[all]>=0.12.0"
- "rich>=13.7.0"

Run: pip install -e ".[cli]" to install dependencies
  </action>
  <verify>
    <automated>python -c "import typer; import rich; print('Dependencies OK')"</automated>
  </verify>
  <done>Typer and Rich installed and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI package structure</name>
  <files>src/tokencunt/cli/__init__.py, src/tokencunt/cli/app.py, src/tokencunt/cli/exit_codes.py</files>
  <action>
Create src/tokencunt/cli/:
- __init__.py: CLI package exports
- app.py: Typer app with global options (-q, -v, --json, --debug, --config)
- exit_codes.py: Exit code constants (SUCCESS=0, ERROR=1, INVALID_ARGS=2, BUDGET_EXCEEDED=3, INTERRUPT=130)

Structure:
- app.py creates main Typer() instance
- Global options added via app.callback()
- Shared state stored in typer.Context.obj
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli import app; print('CLI app loads')"</automated>
  </verify>
  <done>CLI package structure exists with app.py, __init__.py, exit_codes.py</done>
</task>

<task type="auto">
  <name>Task 3: Create Rich formatters</name>
  <files>src/tokencunt/cli/formatters.py</files>
  <action>
Create src/tokencunt/cli/formatters.py with:

1. OutputFormatter class:
   - init takes verbosity (-q/-v), json_mode, debug_mode
   - Methods: success(), error(), warning(), info(), print_response()

2. Token display helpers:
   - format_token_cost(estimated, actual) -> str
   - format_budget_status(usage, limit) -> Table

3. Panel helpers:
   - error_panel(title, message, suggestion) -> Panel
   - response_panel(content) -> Panel

Use Rich's Panel, Table, console.print, Console with color system
  </action>
  <verify>
    <automated>python -c "from tokencunt.cli.formatters import OutputFormatter; print('Formatters OK')"</automated>
  </verify>
  <done>OutputFormatter class with all methods exists</done>
</task>

</tasks>

<verification>
- [ ] Typer and Rich import without errors
- [ ] app.py creates Typer instance
- [ ] Global flags (-q, -v, --json, --debug) registered
- [ ] Exit codes defined
- [ ] OutputFormatter ready for command use
</verification>

<success_criteria>
CLI package structure complete with working entry point and Rich formatters ready for commands.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-tool/02-01-SUMMARY.md`
</output>
